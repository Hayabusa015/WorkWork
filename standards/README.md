# standards — the rules

| File | Owns |
|---|---|
| `ANTI_AI_SLOP_STANDARD.md` | The binding quality standard: documents, slides, voice, and interactive output |
| `VOICE.md` | How Mr. Shull writes to his own students |
| `NAMING.md` | `SHULL_[COURSE]_[Type]_U##_S##.#[_Descriptor][_Version].[ext]`, zero-padded |
| `DRIVE_ARCHITECTURE.md` | The Drive tree, folder grammar, and routing rules |
| `QA_GATE.md` | The nine checks every deliverable passes before it ships |
| `superseded/` | Proposals considered and declined, kept so they are not re-proposed |

Skills point at these. Standards do not restate each other.

**The anti-slop standard is the highest-value item in the migration.** In the legacy system it lived
in Claude Project Knowledge, which Claude Code cannot read, and the fallback file the router pointed
at did not exist — so the system's most important quality rule was unreachable by the skill that
mandated it. The verbatim source is already in `legacy/drive-standards/`.

*Status: not yet written. Phase 8.*
