# Drive operation log — CHEM U01 S01.4 Slides

**Logged:** 2026-09-08 · **Agent:** Librarian · **Task:** T-7 acceptance test
**Status:** **LOGGED, NOT EXECUTED — awaiting the user's go-ahead**

Logged before execution, per `standards/DRIVE_ARCHITECTURE.md` §3. A mutation that was not
logged cannot be rolled back, so the log comes first even when the operation is then held.

## Why it is held

These three operations write into Matthew's **live teaching Drive**. The deck is a real
deliverable, not a throwaway, but it has not been reviewed by him. Filing an unreviewed deck
into the folder he teaches out of is his call, not mine. Nothing below has run.

## Planned operations, in order

| # | Operation | Target | Parent | Rollback |
|---|---|---|---|---|
| 1 | `create_file` (folder) | `Section 01.4 - Isotopes` | `1vYISUV443AAVTmODSnUIwxSUgxCc5i8b` | `trash_file` the new folder |
| 2 | `create_file` (folder) | `Presentations` | the folder from step 1 | `trash_file` the new folder |
| 3 | `create_file` | `SHULL_CHEM_Slides_U01_S01.4.pptx` | the folder from step 2 | `trash_file` the new file |

## Pre-state, verified by lookup

| Object | ID | State |
|---|---|---|
| `Chemistry/` | `1dA5kKA9vQxaWX0CZMhXIoBMYGfDhMxH6` | exists, 16 unit folders |
| `Chemistry/Unit 01 - Matter & Atomic Structure/` | `1vYISUV443AAVTmODSnUIwxSUgxCc5i8b` | exists, **empty** |
| `Section 01.4 - Isotopes/` | — | does not exist |

The empty unit folder is **not** a defect. `DRIVE_ARCHITECTURE.md` §1: "Section subfolders are
created on demand, when first needed." This is the first need.

Only `Presentations/` is created, not all five content folders — the same rule. The other four
are created when something is first filed into them.

## Grammar check, before creating anything

- `Section 01.4 - Isotopes` — zero-padded, hyphen-minus separator, **not an en dash**. An en dash
  creates a near-duplicate folder that sorts beside the real one and quietly splits a section's
  materials in two.
- `Presentations` — one of the five exact names. Not `Slides`, not `Presentation`.
- Section title `Isotopes` matches `courses/chemistry/DECISIONS.md` exactly.
- No existing folder matches case-insensitively, so nothing is being duplicated.

## Verification required after execution

Not "the create call returned success." An **independent `parentId` lookup** on the returned file
ID, confirming the object is in the folder it is claimed to be in. The confirmed path and ID go
into the task report's `location`, with `verifiedBy: parentId lookup`.

Until that lookup runs and confirms, the task report's `librarian` step is not `pass`.
